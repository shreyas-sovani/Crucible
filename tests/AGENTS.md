# AGENTS.md — tests/

## Ownership
Public-interface behavior verification for Crucible backend modules.

## Purpose
Defines pytest specifications for backlog ticket invariants without testing private implementation details.

## What This Controls
Regression safety. A missing or weak test can allow catalog, Event Envelope, anti-leakage, evaluation, or policy constraints to silently drift.

## Connections
- Depends on: source code under [`../src/`](../src/) and dev dependencies in [`../pyproject.toml`](../pyproject.toml).
- Depended on by: ticket promotion in [`../docs/BACKLOG.md`](../docs/BACKLOG.md); CI once added.
- External systems touched: none.

## Current State
Working cross-module behavior suite: twenty-eight pytest tests cover ontology, Event boundary, deterministic background (per-user entities, truncated-normal amounts, 80% IST business hours), topology, eight behavioural crews, chargeback availability, world-scaled fraud volume with unique ids, Train-window resimulation, causal features (vectorized windows exactly matching a loop reference), LightGBM policy/1% FPR metrics, zero-day and delayed-label evaluation, missed-row-only mutation, and both completed and streamed real Cycle HTTP surfaces including fidelity gates, per-family efficacy, evidence samples, and server-side Cycle protection (409 on concurrent trigger, 429 on rate breach, lock release after success/failure).

## Decision Log

### 2026-08-21 — Cycle protection regressions (CRU-21)
- **Change**: `test_api.py` adds three tests: a real in-flight Cycle rejects a second `POST /api/cycle` and `POST /api/cycle/stream` with 409 (polling `cycle_guard.in_flight` for determinism, separate TestClients for thread safety), a `CycleGuard(max_starts=1)` swap proves 429 after one start, and a patched exploding `run_closed_loop` proves the lock releases after failure and the next Cycle succeeds (`raise_server_exceptions=False` to observe the 500).
- **Reasoning**: Protection must be proven at the HTTP boundary against direct API calls, not the browser; the release paths (success, failure) both need locks observed free afterwards.
- **Rejected alternative(s)**: Sleeping-based race tests — flaky. Mocking the guard instead of the orchestrator — would not prove endpoints wire protection to real Cycle execution.
- **Task/session**: Protect expensive Cycle operation on deployed Oracle VM.

### 2026-08-20 — Spec-scale and judge-evidence regressions (CRU-14–20)
- **Change**: `test_background.py` asserts per-user entity reuse and 0.80 ± 0.05 IST share; `test_simulation.py` asserts fraud volume scaling with unique `event_id`s; `test_features.py` keeps a full loop reference for the four windows plus capped `prior_tx_count`; `test_api.py` asserts `fidelity_pass`, gate count, IST band, delayed share ∈ [0.25, 0.35], per-family zero-day rows, and evidence catch/miss contracts.
- **Reasoning**: Every new judge-facing claim needed a public-interface lock: fidelity verdicts must not flake on small samples, efficacy must include the zero-day families, evidence must be token-only, and scaling must not duplicate Event ids.
- **Rejected alternative(s)**: Snapshotting whole Cycle JSON — over-locks model numbers that legitimately move with seeds. Timing-based scale tests — flaky.
- **Task/session**: Spec-scale technical-gap sweep ahead of feature freeze.

### 2026-08-20 — Locked real Cycle-stage stream contract
- **Change**: Extended `test_api.py` to assert ordered Identify, Simulate, Detect, Mutate SSE events followed by a real final Cycle artifact.
- **Reasoning**: A status-only stream could be simulated; this test proves endpoint ordering and that final output remains generated by the real offline Cycle.
- **Rejected alternative(s)**: Testing API helper functions directly — misses HTTP headers, packet shape, and route behavior. Accepting a final artifact without stage events — reintroduces visual no-op risk.
- **Task/session**: Make full offline Cycle visibly unfold from real work.

### 2026-08-20 — Added honest-cycle regressions
- **Change**: Extended crew, simulation, evaluation, detector, mutator, and API tests for distinct behaviours, 30%/45-day card labels, UPI immediacy, label-observed training sets, empirical operating points, legitimate-only KS thresholds, Train-only resimulation, and missed-row SHAP.
- **Reasoning**: Prior tests passed despite generic crews, contaminated fidelity, unavailable card labels in fit, a ranking-only recall number, and duration-based resimulation.
- **Rejected alternative(s)**: Snapshotting a preferred Cycle report or forcing mutation — such tests would conceal whether invariants hold for real seeded artifacts.
- **Task/session**: Repair technical gaps in full Identify → Simulate → Detect → Mutate loop.

### 2026-08-20 — Locked complete Cycle regression at HTTP boundary
- **Change**: Extended `test_api.py` to run default seeded Cycle and assert active crews, fraud overlays, split evidence, decisions, SHAP, and real mutation/retraining result.
- **Reasoning**: Original regression only proved endpoint returned 200/counts; it could pass while every detector and loop module remained disconnected.
- **Rejected alternative(s)**: Mocking orchestrator internals — fails to prove wired process. Accepting arbitrary mutation status — misses default demo regression where evidence is required.
- **Task/session**: Diagnose and complete no-op Run Offline Cycle.

### 2026-08-20 — Added refreshed-PRD vertical-slice suite
- **Change**: Added one public-interface test file for each backend module boundary and maintained RED-to-GREEN cycles per behavior.
- **Reasoning**: Tests verify observable invariants such as PAN rejection, causality, labels, policy bands, and HTTP responses rather than implementation helpers.
- **Rejected alternative(s)**: Bulk test generation before code — TDD vertical slices exposed dependency and integration gaps earlier.
- **Task/session**: Full refreshed-spec TDD implementation.

### 2026-08-20 — Added CRU-01 behavior test
- **Change**: Added `test_load_ontology_returns_complete_simulation_catalog` using public loader rather than YAML parsing internals.
- **Reasoning**: Ticket asks for observable catalog behavior; validating public output protects consumers even if parser internals change.
- **Rejected alternative(s)**: Testing raw YAML keys directly — it would bypass Pydantic validation. Testing private path helper — refactors would break test without changing contract.
- **Task/session**: Project initialization and TDD CRU-01 delivery.

## Known Gotchas
Keep tests at module interfaces. `TestClient` emits a FastAPI deprecation warning from installed Starlette; it is not a behavior failure. Run `uv run pytest -q` from repo root and `npm test` inside `frontend/`. Default Cycle regression uses seed `1`; it must not require a mutation because no eligible policy miss is a valid outcome.
